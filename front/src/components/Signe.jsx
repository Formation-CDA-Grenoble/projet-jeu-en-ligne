import React, { Component } from 'react';
import Axios from 'axios';



class Signe  {
    State= {
        case=case,
        signe=null,
        move_order =0}

      fonction move (State) {
      this.setState.map=>{
          case=this.case,
          signe=this.signe,
          move_order++
      } 
      }

    Axios.post(localhost:3000\State)
    .then(response => 
      
      
         
};